# ADF assets

After building pl_ingest_hydrology in ADF Studio, version-control it here:

1. Best: connect the factory to this GitHub repo (Manage -> Git
   configuration) so every publish commits pipeline JSON automatically.
2. Alternative: Manage -> ARM template -> Export, commit the JSON here.

Interview line this buys you: "The factory is Git-integrated, so pipeline
changes go through pull requests like any other code."
